{{- if .Values.ingress.enabled -}}
apiVersion: traefik.io/v1alpha1
kind: ServersTransport
metadata:
  name: {{ include "common.fullname" . }}-skip-verify
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
spec:
  insecureSkipVerify: true
{{- end }}
