kubernetes-dashboard:
  service:
    externalPort: 443

ingress:
  enabled: true
  host: kube.spitikos.dev
  serviceName: kube-dashboard-kong-proxy
  servicePort: 443
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"

serviceAccount:
  create: false
  name: admin-user
