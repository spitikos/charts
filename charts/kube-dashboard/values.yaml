kubernetes-dashboard:
  service:
    externalPort: 443

ingress:
  enabled: true
  host: kube.spitikos.dev
  serviceName: kubernetes-dashboard-wrapper-kubernetes-dashboard
  servicePort: 443
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"

serviceAccount:
  create: false
  name: admin-user
