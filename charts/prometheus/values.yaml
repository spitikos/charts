kube-prometheus-stack:
  grafana:
    enabled: false
  alertmanager:
    enabled: false
  prometheusOperator:
    enabled: true
  nodeExporter:
    enabled: true
  prometheus:
    enabled: true

ingress:
  enabled: true
  host: prometheus.spitikos.dev
  serviceName: prometheus-kube-prometheus-prometheus
  servicePort: 9090
