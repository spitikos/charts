vault:
  ui:
    enabled: true

  server:
    dev:
      enabled: true

  ingress:
    enabled: true
    ingressClassName: "nginx"
    hosts:
      - host: vault.spitikos.dev
        paths:
          - path: /
    annotations:
      nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
