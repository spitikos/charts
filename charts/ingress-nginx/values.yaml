ingress-nginx:
  controller:
    resources:
      requests:
        cpu: 100m
        memory: 90Mi
      limits:
        cpu: 1000m
        memory: 512Mi
    hostPort:
      enabled: false
    logLevel: "DEBUG"
    config:
      use-forwarded-headers: "true"
